<!--
  News items shown on the home page, newest first (see `announcements:` in _pages/about.md).

  Add one Markdown file per item, named YYYY-MM-DD-slug.md, with:

      ---
      layout: post
      date: 2026-01-31
      inline: true          # false renders a full page with its own title
      related_posts: false
      ---

      One or two sentences. Markdown links are fine.

  The seeded items are dated from the `month` field of the matching entry in
  _bibliography/papers.bib, using the first of that month -- journals give a month, not a day.
  Replace or re-date them freely; they are examples as much as content.

  This README is excluded from the build by `exclude:` in _config.yml.
-->
